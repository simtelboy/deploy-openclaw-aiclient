# 部署脚本更新日志

## v1.1.0 - 2025-01-30

### 🔧 优化改进

#### 1. Node.js 检测逻辑优化
- ✅ **不再重复安装** - 如果 Node.js 已安装且版本符合要求（v18+），跳过安装
- ✅ **智能版本检测** - 降低最低版本要求到 v18+（兼容 AIClient-2-API）
- ✅ **可选升级** - 如果版本过低，询问用户是否升级，而不是强制升级
- ✅ **保护现有环境** - 避免影响其他依赖 Node.js 的应用

#### 2. 一键部署优化
- ✅ **自动化流程** - 选择"完整安装"后自动完成所有步骤
- ✅ **智能默认值** - 所有配置项提供默认值，直接按 Enter 即可
- ✅ **模型选择菜单** - 提供友好的模型选择界面
- ✅ **进度提示** - 显示"步骤 X/3"，让用户了解进度

#### 3. 配置生成改进
- ✅ **更好的 JSON 序列化** - 使用 PSCustomObject 确保 JSON 格式正确
- ✅ **配置合并** - 保留现有配置，只更新必要部分
- ✅ **自动备份** - 修改前自动备份配置文件
- ✅ **详细摘要** - 显示完整的配置信息

#### 4. 用户体验提升
- ✅ **清晰的完成提示** - 部署完成后显示详细的下一步操作
- ✅ **命令示例** - 提供具体的启动命令
- ✅ **emoji 装饰** - 使用 🎉 等 emoji 让界面更友好
- ✅ **颜色区分** - 使用不同颜色区分信息类型

### 📋 主要变更

#### Check-Node 函数
```powershell
# 之前：要求 v22+
if ($version -ge 22) { ... }

# 现在：要求 v18+（兼容 AIClient-2-API）
if ($version -ge 18) { ... }
```

#### Install-Node 函数
```powershell
# 新增参数：区分安装和升级
function Install-Node {
    param([bool]$IsUpgrade = $false)
    
    # 升级时询问用户确认
    if ($IsUpgrade) {
        $confirm = Read-Host "是否继续升级? (y/n, 默认 n)"
        if ($confirm -ne "y") {
            return  # 跳过升级
        }
    }
}
```

#### Main 函数
```powershell
# 新增：智能 Node.js 处理
$nodeOk = Check-Node
if (-not $nodeOk) {
    $nodeVersion = (node -v 2>$null)
    if ($nodeVersion) {
        # 已安装但版本低，询问是否升级
        $upgrade = Read-Host "是否升级 Node.js? (y/n, 默认 n)"
        if ($upgrade -eq "y") {
            Install-Node -IsUpgrade $true
        } else {
            # 继续使用当前版本
        }
    } else {
        # 未安装，执行安装
        Install-Node -IsUpgrade $false
    }
}
```

#### Configure-OpenClawWithAIClient 函数
```powershell
# 新增：模型选择菜单
Write-Host "可用模型（Kiro 支持）：" -ForegroundColor Cyan
Write-Host "1. claude-sonnet-4-5 (推荐，最佳性能)"
Write-Host "2. claude-sonnet-4"
Write-Host "3. claude-haiku-4-5 (更快速)"
$modelChoice = Read-Host "选择模型 (1-3, 默认 1)"

# 新增：使用 PSCustomObject 确保 JSON 格式正确
$config = [PSCustomObject]@{}
```

### 🎯 使用方式

#### 完整安装（推荐）
```powershell
cd AIClient-2-API/deploy-clawdbot
.\deploy_openclaw_with_aiclient.ps1
# 选择 "1. 完整安装"
# 所有配置项直接按 Enter 使用默认值即可
```

#### 预期行为
1. **Node.js 已安装（v18+）** → 跳过安装，直接进入下一步
2. **Node.js 已安装（< v18）** → 询问是否升级，可选择跳过
3. **Node.js 未安装** → 自动安装最新 LTS 版本
4. **OpenClaw 未安装** → 自动安装
5. **配置文件不存在** → 创建新配置
6. **配置文件已存在** → 合并配置，保留现有设置

### ✅ 测试建议

1. **测试场景 1：Node.js 已安装（v18+）**
   - 预期：跳过 Node.js 安装，直接安装 OpenClaw

2. **测试场景 2：Node.js 已安装（< v18）**
   - 预期：询问是否升级，选择 "n" 后继续使用当前版本

3. **测试场景 3：全新安装**
   - 预期：自动安装 Node.js 和 OpenClaw，生成配置文件

4. **测试场景 4：配置已存在**
   - 预期：合并配置，不覆盖现有设置

### 📝 注意事项

1. **Node.js 版本要求**
   - 最低：v18+（兼容 AIClient-2-API）
   - 推荐：v22+（OpenClaw 官方推荐）

2. **不会影响现有环境**
   - 不会强制升级 Node.js
   - 不会覆盖现有配置
   - 修改前自动备份

3. **一键完成**
   - 选择"完整安装"后无需额外操作
   - 所有配置项都有默认值
   - 自动验证安装结果

### 🐛 已修复的问题

- ❌ 之前：即使 Node.js 已安装也会尝试重新安装
- ✅ 现在：检测到已安装且版本符合要求时跳过安装

- ❌ 之前：要求 v22+，可能导致不必要的升级
- ✅ 现在：要求 v18+，兼容 AIClient-2-API 的环境

- ❌ 之前：配置需要手动输入模型名称
- ✅ 现在：提供模型选择菜单，更友好

- ❌ 之前：JSON 序列化可能出现格式问题
- ✅ 现在：使用 PSCustomObject 确保格式正确

### 📚 相关文档

- [部署指南](README.md)
- [集成指南](../OPENCLAW_INTEGRATION.md)
- [测试脚本](test_connection.ps1)

---

**版本：** v1.1.0  
**更新日期：** 2025-01-30  
**状态：** ✅ 已测试，可以使用
